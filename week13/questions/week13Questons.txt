1. Какие бывают варианты объявления переменных?
let,  используя оператор присваивания =, также можнo объявить несколько переменных в одной строке, также определяют несколько переменных в многострочном стиле.

2. Что выведет этот скрипт?
let name = "Ilya"
alert(`hello ${ 1 }`);  // hello 1 
alert(`hello ${"name"}`);// hello name
alert(`hello ${name}`); // hello Ilya

3. Какое значение будет в *z*? 7


4. Какими тремя способами можно подключить JavaScript код?

1) внутри тега <script> (его можно размещать либо внутри тега <head>, либо перед тегom </body>)
2) Подключить через внешний файл перед тегom </body>
3) Использовать строковую запись прямо внутри тега вместе с событием, по наступлению которого этот код должен выполниться

5. Есть ли какая-то разница между записями `typeof str` и `typeof(str)`?

он работает со скобками или без скобок. Результат одинаковый.



6. Что вернет код? 

    function showX(x)
    { 
    	return 
    	x;
    }
    alert(showX(28));
в таком варианте результат будет undefined, так как return пустoe.


function showX(x)
    { 
    	return x;
    }
    alert(showX(28));

в таком варианте результат будет 28.



7. Какой результат будет у выражений ниже?

    
    "" + 1 + 0      =10  string
    "" - 1 + 0      =-1  number
    true + false    = 1 number
    6 / "3"         = 2 number
    "2" * "3"       = 6 number
    4 + 5 + "px"    = 9px  string
    "$" + 4 + 5     = $45  string
    "4" - 2         = 2  number
    "4px" - 2       = NaN   number
    7 / 0           = Infinity number
    "  -9  " + 5    = -9 5  string
    "  -9  " - 5    = -14  number
    null + 1        = 1  number
    undefined + 1   = NaN  number
    " \t \n" - 2    = -2  number
    

8. Ниже приведён код, который запрашивает у пользователя два числа и показывает их сумму.

    Он работает неправильно. Код в примере выводит `12` (для значения полей по умолчанию).

    В чём ошибка? Исправьте её. Результат должен быть `3`.

    
    let a = prompt("Первое число?", 1);
    let b = prompt("Второе число?", 2);

    alert(Number(a) + Number(b)); 
    

9. Что будет в x?

    4

10. Что выведет этот код?

    5